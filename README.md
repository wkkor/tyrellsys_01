1. Install Docker and Docker Compose
2. Open Terminal / Command Prompt, go to this folder
3. Run command docker-compose up and leave the Terminal / Command Prompt open
4. Open browser and enter URL http://localhost:10800